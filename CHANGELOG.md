## Unreleased

- Flutter、SwiftUI、Jetpack Composeへ非再帰の軽量な返信参照、長押し返信action、
  Composerの選択・取消、送信済みメッセージ内の引用表示と型付き送信値を追加した。
- 返信元の保存、権限、backend再検証、引用tap後の取得とnavigationは利用アプリへ委譲した。
- Flutterの日付区切りを、利用アプリによるlocale data初期化なしでも表示できるようにした。

## 1.7.1 - 2026-08-29

- SwiftUIとJetpack Composeで、scheme・hostの大文字小文字、標準port、fragmentだけが
  異なる同一URLのresolver結果を保持し、送信時のpreviewへ含めるよう修正した。

## 1.7.0 - 2026-08-29

- Flutter、SwiftUI、Jetpack Composeへ、アプリが解決したWebリンクmetadataを表示する
  共通カードと、入力中の500ミリ秒debounce・古い結果破棄・型付き送信値を追加した。
- Flutterの端末直接OGP取得を削除し、外部取得、Firebase接続、画像解決を利用アプリへ
  委譲する責務へ統一した。

## 1.6.2 - 2026-08-28

- SwiftUIで非表示中に項目を受信したタイムラインを表示する際、再生成前の
  ScrollViewProxyへ初期位置指定が送られて空白になる競合を修正した。

## 1.6.1 - 2026-08-28

- SwiftUIのタイムライン初期位置を、末尾anchorがviewportへ到達するまで保持し、
  非表示中に項目を受信した場合も初期位置を再適用するよう改善した。

## 1.6.0 - 2026-08-27

- SwiftUIとJetpack Composeへ構造化ステッカーメッセージと、読み込み・失敗・再試行を
  含む共通表示を追加した。
- FlutterへCharacter・UTF-16を選択できる入力長方針、上限制御、文字数表示を追加した。
- Flutterへ、権限判定と実際の削除をアプリ側に保ったまま利用できる共通削除確認UIを追加した。

## 1.5.1 - 2026-08-27

- Flutter、SwiftUI、Jetpack Composeで、単一画像の高さ制限付き可変比率を既定にし、
  従来互換の正方形表示を選べるよう統一した。
- 複数画像は3枚時だけ先頭横長またはモザイクを選択し、4枚以上は2×2と超過件数の
  表示へ統一した。
- ComposeへCoilベースの標準画像描画を追加し、各実装でローカル画像からリモート画像へ
  切り替える際の表示維持を標準動作にした。
- Flutterで画像寸法が未指定でも元画像比率を使える表示へ改善し、入力の即時クリア、
  失敗表示の優先、メッセージ順の互換処理を修正した。
- SwiftUIの最新付近判定をpoint単位の閾値へ変更し、Composeの標準追従方針と
  最新へ戻る操作を3実装で統一した。

## 1.5.0 - 2026-08-26

- Flutter、SwiftUI、Jetpack Composeで、送信中・失敗・再送を同じメッセージIDで扱う
  楽観的送信契約を統一した。
- 自分の送信または最新位置付近の新着だけへ追従し、過去閲覧中は位置を維持して
  最新位置へ戻れるタイムライン操作を追加した。
- 複数画像の表示を、先頭横長グリッドとモザイクから選べるようにした。
- 同じ画像IDでローカル画像からリモート画像へ切り替える際、読み込み済み画像を
  維持してちらつきを防ぐ契約を追加した。
- ステッカー入力を明示的に有効化した場合だけ切替操作を表示するよう統一した。

## 1.4.1 - 2026-08-26

- SwiftUIとJetpack Composeのリンク対応本文を公開APIとして提供し、独自の吹き出しや
  カードからも共通のリンク検出・操作を利用できるようにした。

## 1.4.0 - 2026-08-25

- 1.3.5で追加したメッセージ内リンク機能を、新機能追加に適したminor versionとして
  再リリースした。

## 1.3.5 - 2026-08-25

- Flutter、SwiftUI、Jetpack Composeで、通常本文、画像caption、システムメッセージ内の
  Web URL、メールアドレス、電話番号をリンクとして表示し、OS標準アプリで開けるようにした。
- schemeのない電話番号から、電話またはSMSを選択できる操作UIを追加した。

## 1.3.4 - 2026-08-25

- SwiftUI、Jetpack Compose、Flutterのステッカーpickerへ、ロック表示、履歴を含む
  選択抑止、アプリ固有の利用導線callbackを追加。

## 1.3.3 - 2026-08-25

- Jetpack Composeへ、SwiftUI版と同じ型付き初期位置、末尾追従、手動・自動の履歴追加、
  履歴追加時の位置保持を備えた汎用`ChatTimeline`を追加。
- Composeへ日付・未読区切りと削除確認を追加し、既存`AltiveChatRoom`も共通タイムラインへ統合。
- 通常起動、指定項目、履歴追加時の位置と削除確認を実Compose UIで検証する回帰testを追加。

## 1.3.2 - 2026-08-24

- タイムラインの初期位置を末尾または指定項目の型付き方針で指定するAPIを追加。
- 履歴追加後のレイアウト反映を待ち、アニメーションなしで位置を復元するよう改善。
- 削除後にキャンセルcallbackが呼ばれ得る状態遷移を修正。
- 通常起動、指定項目、履歴追加時の位置を実ScrollViewで検証する回帰testを追加。

## 1.3.1 - 2026-08-24

- 共通Room移行前の入力欄余白、区切り表示、履歴読み込みボタン、削除確認形式を
  アプリごとに維持できる設定を追加。

## 1.3.0 - 2026-08-24

- SwiftUIのRoomシェル、手動・自動の履歴追加、履歴追加時の位置保持、日付・未読区切り、
  削除確認を共通化。
- 既存の`AltiveChatRoom`を共通Roomシェルとタイムライン上へ統合。

## 1.2.9 - 2026-08-24

- SwiftUIへ、アプリ固有の行を差し込める汎用タイムラインと、初期位置、末尾追従、
  履歴追加時の位置保持を追加。

## 1.2.7 - 2026-08-22

- Flutter、SwiftUI、Composeで、添付メニュー展開時にキーボードを閉じ、入力状態との競合を解消。

## 1.2.6 - 2026-08-22

- SwiftUI、Composeで、添付メニュー展開時の不要なフォーカス再要求をなくし、展開状態がすぐ閉じる競合を解消。

## 1.2.4 - 2026-08-21

- Flutter、SwiftUI、Composeで、添付メニュー展開時のフォーカス競合を防ぐように修正。

## 1.2.3 - 2026-08-21

- 入力欄のフォーカス中に添付ボタンを展開しても、キーボードを維持するように修正。

## 1.2.1 - 2026-08-20

- 単純なシステムメッセージの本文と時刻を中央寄せ表示に統一。

## 1.2.0 - 2026-08-20

- Flutter、SwiftUI、Composeへ、アプリが内容を構築できる汎用メッセージカードを追加。
- celebrationカードのgradient、境界線、前景色、装飾色を共通themeで変更可能にした。

## 1.1.0 - 2026-08-13

- Flutter、SwiftUI、Composeで画像と本文を1メッセージとして表示できるようにした。

## 1.0.0 - 2026-08-12

- iOS・Androidの写真ライブラリを、OS標準の複数選択シート表示へ統一。
- 旧inline写真Pickerの公開API、状態、レイアウト計算を削除。
- Android向けに純Kotlinの`chat-core`とJetpack Composeの`chat-ui-compose`を追加。
- SwiftUI版と同じテキスト/システム表示、入力、送信状態、再送、リアクション、
  ステッカーpicker、アバター、イベント展開、タイムライン境界を提供。
- SwiftUI 向け `AltiveChatUI` パッケージの初期基盤を追加。
- Flutter パッケージを維持したまま、マルチプラットフォーム構成へ移行。

## 0.0.1

* TODO: Describe initial release.
